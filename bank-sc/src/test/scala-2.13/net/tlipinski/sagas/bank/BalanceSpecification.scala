package net.tlipinski.sagas.bank

import org.scalacheck.Prop.{forAll, propBoolean}
import org.scalacheck._

object BalanceSpecification extends Properties("String") {
//  property("aaa") = forAll { amount: Int =>
//    (amount >= 0) ==> (
//      Balance(1000, 0, 0).add(amount) match {
//        case Some(b) => b.balance >= 0
//        case None    => false
//      }
//    )
//  }
//
//  property("bbb") = forAll { amount: Int =>
//    (amount >= 0) ==> (
//      Balance(1000, 0, 0).sub(amount) match {
//        case Some(b) => b.balance - b.pendingSub >= 0
//        case None    => amount > 1000
//      }
//    )
//  }
}
